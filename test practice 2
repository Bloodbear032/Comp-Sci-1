#include <iostream>
#include <iomanip>
using namespace std;

bool isPrime(int value);
int reverse(int value);

int main() {

	int user_input = 0;
	do {
		cout << "Please enter a positive integer: ";
		cin >> user_input;
	} while (user_input < 0);
}

bool isPrime(int value)
{
	value /= value;

	if (value > 1) {
		return true;
	}
	else {
		return false;
	}
	
}

int reverse(int value)
{
	return 0;
}
